# Data access

Put feature-specific Supabase query modules in this directory. Server Components
and Server Actions may call this layer; presentational components must not import
Supabase clients directly.
